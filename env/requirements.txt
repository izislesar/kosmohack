# Pinned runtime for kosmohack fire-win-19h (Task 1).
# Torch MUST match CUDA: cu121 wheels for torch 2.3.* (driver 595 / CUDA 13.2 is backward-compatible).
# Install order matters — see env/bootstrap.sh:
#   1) pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cu121
#   2) pip install -r requirements.txt   (this file, PyPI only)
# Lean runtime: NO jupyterlab/notebook, NO openjev.
torch==2.3.1
torchvision==0.18.1
segmentation-models-pytorch==0.5.0
timm==1.0.15
terratorch==1.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
huggingface_hub==0.26.5
hf_transfer==0.1.8
numpy==1.26.4
rasterio==1.3.11
tifffile==2024.8.30
pyyaml==6.0.2
pillow==10.4.0
scipy==1.14.1
scikit-learn==1.5.2
opencv-python-headless==4.10.0.84
albumentations==1.4.6
einops==0.8.0
